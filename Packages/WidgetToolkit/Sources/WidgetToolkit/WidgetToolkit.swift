/// Walking-skeleton entry point (E0.2). Rollover/stale-grace/timer-text helpers and
/// the InstantLaunch module arrive in E6.1/E3.1, test-first.
public enum WidgetToolkit {
    /// Proves the package links and exposes an entry point (E0.2 acceptance).
    public static let version = "0.0.1-skeleton"
}
