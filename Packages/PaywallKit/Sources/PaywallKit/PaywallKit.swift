/// Walking-skeleton entry point (E0.2). The entitlement state machine and the
/// removable Superwall adapter arrive in E7.1/E7.2, test-first.
public enum PaywallKit {
    /// Proves the package links and exposes an entry point (E0.2 acceptance).
    public static let version = "0.0.1-skeleton"
}
